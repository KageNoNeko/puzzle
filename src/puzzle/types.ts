export interface Size {
    width: number;
    height: number;
}

export interface Point {
    x: number;
    y: number;
}

export interface PuzzleCell {
    row: number;
    column: number;
}

export interface PuzzleDimension {
    rows: number;
    columns: number;
}